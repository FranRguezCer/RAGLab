# Debugging Pico software

The Serial Wire Debug interface supports program loading and source-level debugging. A Raspberry Pi Debug Probe or a second Pico running `debugprobe` can bridge USB to SWD and UART. `picotool` can inspect metadata embedded in binaries built with the Pico C/C++ SDK.
