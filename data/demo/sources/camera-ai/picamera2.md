# Picamera2

Picamera2 is the supported Python camera library built on libcamera. Install it through Raspberry Pi OS packages to retain system compatibility. Applications can configure still or video streams, start the camera, and capture arrays or files. AI Camera examples add model-specific tensor post-processing.
